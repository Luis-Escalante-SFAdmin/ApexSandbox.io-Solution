public String divide(Integer a, Integer b){ 
    try
    {
        Integer division = String.valueOf(a / b);
        String result = division;
        
    }
    catch (Exception e)
    {
        System.debug('Error: ' + e.getMessage());
    }
    return result;
}