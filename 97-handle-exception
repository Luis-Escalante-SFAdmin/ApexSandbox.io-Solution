public String divide(Integer a, Integer b){ 
    try
    {
        String division = String.valueOf(a / b);
        String result = division;

        if(Math.mod(a, b) == 0)
        {
            return null;
        }
        
    }
    catch (Exception e)
    {
        return e.getMessage();
    }
    return result;
}