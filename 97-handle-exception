public String divide(Integer a, Integer b){ 
    try
    {
        Integer division = divide(a, b);
        String result = String.valueOf(division);
        
    }
    catch (Exception e)
    {
        System.debug('Error: ' + e.getMessage());
    }
    return result;
}