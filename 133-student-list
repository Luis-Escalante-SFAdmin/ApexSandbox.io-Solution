public void insertStudents(List<String> studentNames, List<String> studentEmails) {
    List<apxio__Student__c> students = [SELECT Id, Name, apxio__Email__c FROM apxio__Student__c WHERE Name= studentNames AND apxio__Email__c = studentEmails LIMIT 150];
    students.add(i);
    for(Integer i = 0; i < students.size(i); i++){
        System.debug(i);
    }

    insert students;
}