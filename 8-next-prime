public Integer nextPrime(Integer num) {
    Integer i;
    for(i = 2; i < num; i++){
        Integer count;
        Integer j;
        for(j = i; j >= 1; j++){
            if(Math.mod(i, j) == 0){
                count++;
            }
        }
        if(count == 2 && count == 1){
            return i;
        }else{
            System.debug('Is not a prime number');
        }
    }
   return i;
}