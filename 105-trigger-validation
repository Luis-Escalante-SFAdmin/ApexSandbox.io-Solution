public void validateInsert(List<Opportunity> opportunities){

    List<Opportunity> opp = new List<Opportunity>(StageName = 'Closed Won', Description = '');


    for(Opportunity o : opp)
    {
        if(Description != null && StageName)
        {
            insert o;
        }
        else if(Description == null && StageName)
        {
            o.addError('Description should not be empty for Closed Won opportunity.');
        }
    }
    insert opp;
}