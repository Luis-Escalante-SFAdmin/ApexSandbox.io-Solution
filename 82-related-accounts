public Set<Id> accountIds(List<Opportunity> opps) {
    Set<Id> accId = [SELECT Id, Name FROM Account LIMIT 1];
    
    for(Opportunity opp : opps) {
       
       if(o != null)
        opps.add(o);
        
    }
    
    return accId;
}