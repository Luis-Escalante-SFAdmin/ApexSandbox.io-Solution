public Set<Id> accountIds(List<Opportunity> opps) {
    Set<Id> accId = new Set<Id>();
    
    Account[] a = new Account[]{};
    for(Opportunity opp : opps) {
       Account acc = [SELECT Id, Name, (SELECT AccountId FROM Opportunities) FROM Account LIMIT 1];
           
           a.add(acc);
       
    }
    
    return accId;
}