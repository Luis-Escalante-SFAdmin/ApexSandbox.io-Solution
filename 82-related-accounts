public Set<Id> accountIds(List<Opportunity> opps) {
    Set<Id> accId = new Set<Id>();
    

    for(Opportunity opp : opps) {
       opp = [SELECT Id, Name FROM Opportunity WHERE Id = :accId LIMIT 1];
       if(opp != null) {
           accId.add(opp.Id);
       }
    }
    
    return accId;
}