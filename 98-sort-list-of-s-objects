public List<Account> getAccounts(List<Account> accounts) {

    accWrapper = new List<AccountWrapper>();
    for(Account a : accounts)
    {
        accWrapper.add(new AccountWrapper(a));
    }
    accWrapper.sort();
    return accWrapper;
}
// write extra class here 
public class AccountWrapper implements Comparable
{
    public Account account {get; set;}
    public Decimal AnnualRevenue {get; set;}

    public AccountWrapper(Account a)
    {
        account = a;
        Decimal revenue = a.AnnualRevenue;
    }

    public Integer compareTo(Object compareTo)
    {
        AccountWrapper otherAcc = (AccountWrapper)compareTo;
        if(revenue == a.AnnualRevenue)
        {
            return 0;
        }
        else if(revenue < a.AnnualRevenue)
        {
            return -1;
        }
        else
        {
            return 1;    
        }
    }
}