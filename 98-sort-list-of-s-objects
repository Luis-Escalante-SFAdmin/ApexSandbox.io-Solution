public List<Account> getAccounts(List<Account> accounts) {

List<AccountWrapper> accWrapper = new List<AccountWrapper>();
    
    for(Account a : accounts)
    {
        AccountWrapper account = new AccountWrapper();

        account.a = a;
        account.AnnualRevenue = a.AnnualRevenue;
        accWrapper.add(account);    
    }
    accWrapper.sort();
    
    List<Account> sorted = new List<Account>();

    for(AccountWrapper aWrap : accWrapper)
    {
        sorted.add(aWrap.a);
    }

    return sorted;
}
// write extra class here 
public class AccountWrapper implements Comparable
{
    public Account a;
    public Decimal AnnualRevenue;

    public Decimal compareTo(Object obj)
    {
        AccountWrapper otherAcc = (AccountWrapper)obj;

        if(this.AnnualRevenue == otherAcc.AnnualRevenue)
        {
          return 0;
        }
        else if(this.AnnualRevenue > otherAcc.AnnualRevenue)
        {
            return 1;
        }
        return -1;
    }

}