public List<Account> getAccounts(List<Account> accounts) {

    accounts.sort(new AccountWrapper());
    return accounts;
}
// write extra class here 
public class AccountWrapper implements Comparable
{
    public Integer compareTo(Object compareTo)
    {
        AccountWrapper otherAcc = (AccountWrapper)compareTo;

        Decimal annualRevenue;
        Integer returnValue = 0;
        if((annualRevenue == null) && (otherAcc.annualRevenue == null))
        {
            returnValue = 0;
        }
        else if((annualRevenue == null) && (otherAcc.annualRevenue != null))
        {
            returnValue = -1;
        }
        else if((annualRevenue != null) && (otherAcc.annualRevenue == null))
        {
            returnValue = 1;
        }
        else if(annualRevenue > otherAcc.annualRevenue)
        {
            returnValue = 1;
        }
        else if(annualRevenue < otherAcc.annualRevenue)
        {
            returnValue = -1;
        }
        return returnValue;
    }
}