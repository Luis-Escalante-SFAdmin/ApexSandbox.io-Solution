public List<Account> getAccounts(List<Account> accounts) {

List<Account> accountList = [SELECT Name, AnnualRevenue FROM Account ORDER BY AnnualRevenue DESC];
        
        List<AccountWrapper> accWrapperList = new List<AccountWrapper>();
        
        for (Account a : accountList) {
            accWrapperList.add(new AccountWrapper(a));
        }
        
        accWrapperList.sort();
        
        return accWrapperList;
    }
// write extra class here 
public class AccountWrapper implements Comparable
{
    public Account acc;
    public Integer AnnualRevenue;

    public Integer compareTo(Object obj)
    {
        AccountWrapper otherAcc = (AccountWrapper)obj;

        if(this.AnnualRevenue == otherAcc.AnnualRevenue)
        {
          return 0;
        }
        else if(this.AnnualRevenue > otherAcc.AnnualRevenue)
        {
            return -1;
        }
        return 1;
    }

}