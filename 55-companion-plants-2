public Boolean companionPlants(List<String> plants) {

    for(String plant : plants)
    {
        if((plant == 'lettuce' && plant == 'cucumbers') && (plant == 'onions' && plant == 'cucumbers'))
        {
            return true;
        }
        else if((plant == 'onions' && plant == 'carrots') && (plant == 'tomatoes' || plant == 'onions'))
        {
            return true;
        }
        else
        {
            return false;
        }
    }
    
    return true;
}