public Boolean companionPlants(List<String> plants) {

    if(plants.isEmpty()) return true;

    if(!plants.isEmpty())
    {
    for(Integer i = 0; i <= plants.size() + 1; i++)
    {
        if(plants[i] != plants[i + 1])
        {
            return true;
        }
        else if(plants[i] == plants[i])
        {
            return false;
        }
    }
    }
    return false;
}