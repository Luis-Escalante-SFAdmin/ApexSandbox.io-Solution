public Boolean companionPlants(List<String> plants) {

    if(plants.isEmpty()) return true;
    
    if(!plants.isEmpty()) return false;
    
    for(Integer i = 0; i >= plants.size(); i++)
    {
        if((plants[i - 1] == plants[i - 1]) || (plants[i - 1] != plants[i + 1]))
        {
            return true;
        }
    }
    return false;
}