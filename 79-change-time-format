public String changeTimeFormat(String strTime) {
    String hours = strTime.substringBefore(':');
    Integer hours2 = Integer.valueOf(hours);

    if(hours2 >= 13 || hours2 <= 23){
        return hours2 = hours2 - 12;
    }
    return strTime.format(hours2);
}