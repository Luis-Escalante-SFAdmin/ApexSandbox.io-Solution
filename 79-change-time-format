public String changeTimeFormat(String strTime) {

    Integer hours2 = Integer.valueOf(strTime);


    if(hours2 >= 13 && hours2 <= 24){   
        hours2 = hours2 - 12;
        return strTime + ' PM'; 
    } else if(hours2 >= 00 && hours2 <= 11){
        hours2 = hours2;
        return hours + ':' + minutes + ' AM';
    }
    
    return hours + ':' + minutes + ' PM';
}