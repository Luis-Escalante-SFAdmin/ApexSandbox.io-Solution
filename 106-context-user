public Map<String,String> getContextUserInformation(){
    Map<String, String> info = new Map<String, String>();

    info = userInfo.getUserEmail();
    info = userInfo.getUserName();
    info = userInfo.getUserType();
    info = userInfo.getProfileId();
    
    return info;
}