public Map<String,String> getContextUserInformation(){
    String info = new String();

    
    userInfo.getUserEmail(info);
    userInfo.getUserName(info);
    userInfo.getUserType(info);
    userInfo.getProfileId(info);
    
    return info;
}