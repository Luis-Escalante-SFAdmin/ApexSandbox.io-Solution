public String nameFromEmail(String email) {
    String firstName = email.toLowerCase();
    String lastName = email.toLowerCase();
    
    String fullName = firstName.substringBefore('.') + ' ' + lastName.substringBefore('@');
    
    return fullName.capitalize();
}