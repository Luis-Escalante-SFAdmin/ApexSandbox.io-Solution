public String nameFromEmail(String email) {
    String firstName = email.toLowerCase(substringBefore('.'));
    String lastName = email.toLowerCase(substringAfter('.'));
    
    return firstName.capitalize() + lastName.capitalize();
}