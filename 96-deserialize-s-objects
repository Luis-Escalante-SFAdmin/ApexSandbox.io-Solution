public List<Account> getAccountsFromJSONString(String inputJSON){

    List<Account> accounts = new List<Account>();

    if(inputJSON != null)
    {
        Account acc = (Account)JSON.deserialize(inputJSON, Account.class);
    }
    else
    {
        return null;
    }

    return accounts;
}