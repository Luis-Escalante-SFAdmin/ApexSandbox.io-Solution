public boolean isSorted(List<Integer> numbers) {
    Set<Integer> sortNum = new Set<Integer>();

    for(Integer num : numbers){
        for(Integer i = 0; i < numbers.size(); i++){

            Boolean result = sortNum.contains(num);

            if(result != null) {
                return true;
            }
            else{
                return false;
            }
    }
    }
   return false; 
}