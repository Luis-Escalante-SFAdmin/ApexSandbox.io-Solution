public boolean isSorted(List<Integer> numbers) {
    Integer[] sortNum =  new Integer[]{};

    for(Integer num : sortNum){
        for(Integer i = 0; i < numbers.size(); i++){
            if(num.contains(i - 1) != num){
                return true;
            }
            else{
                return false;
            }
        }
    }
   return true; 
}