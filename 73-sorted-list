public boolean isSorted(List<Integer> numbers) {

    if(numbers.sort() == true){
        return true;
    }

    for(Integer num : numbers){
        for(Integer i = 0; i < numbers.size(); i++){
            if(num < 3) {
                return true;
            }
            else{
                return false;
            }
    }
    }
   return true; 
}