public boolean isSorted(List<Integer> numbers) {

    for(Integer num : numbers){

        for(Integer i = 1; i < numbers.size(); i++){

            if(numbers[i - 1] > num){
                return false;
            }
            else if(numbers[i] < 3){
                return false;
            }
            
    }
    }
   return true; 
}