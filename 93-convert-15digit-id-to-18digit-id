public String convert15to18DigitId(String fifteenDigit)
{
    String eightingChar = fifteenDigit.length();

    if(eightingChar == fifteenDigit){
        return newId;
    }
    else
    {
        system.debug('You need an ID');
    }
    return '-1';

}