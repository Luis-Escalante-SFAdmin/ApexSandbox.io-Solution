public String convert15to18DigitId(String fifteenDigit)
{
    Id newId = Id.valueOf(fifteenDigit);
    String moreId = newId;

    if(moreId == newId){
        return newId;
    }

    return '-1';

}