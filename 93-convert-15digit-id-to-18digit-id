public String convert15to18DigitId(String fifteenDigit)
{
    List<Id> newId = new List<Id>(fifteenDigit);

    if(newId.length = 15){
        return newId;
    }
    else
    {
        return -1;
    }


}