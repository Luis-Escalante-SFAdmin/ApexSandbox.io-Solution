public Integer fibonacci(Integer n) {

    if(n >= 1){
        n = n + 0;
    }
    if(n >= 2){
        n = n + 1;
    }

    for(Integer i = 2; i < n; i++){
        Integer nextNum = n[i - 1] + n[i - 2];
        n = n + nextNum;
    }

    return nextNum;
}