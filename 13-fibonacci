public Integer fibonacci(Integer n) {
    Integer fibSeq = new List<Integer>();
    fibSeq.add(1);
    fibSeq.add(2);

    for(Integer i = 3; i < n; i++){
        Integer nextNum = fibSeq[i - 2] + fibSeq[i - 3];
        fibSeq.add(nextNum);
    }

    return n;
}