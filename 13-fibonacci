public Integer fibonacci(Integer n) {
    List<Integer> fibSeq = new List<Integer>();
    fibSeq.add(1);
    fibSeq.add(2);

    for(Integer i = 2; i < n; i++){
        Integer nextNum = fibSeq[i - 1] + fibSeq[i - 2];
        fibSeq += nextNum;
    }

    return n;
}