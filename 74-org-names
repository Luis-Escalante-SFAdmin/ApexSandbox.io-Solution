public List<String> orgNames(List<String> adjectives, List<String> animals) {
    List<String> orgs = new List<String>();

    for(String o : orgs)
    {        
        for(Integer i = 0; i < adjectives.size(); i++)
        {
            for(Integer j = 0; j < animals.size(); j++)
            {
                o.add(adjectives[i]animals[j])
            }
        }
    }
    return orgs;
}