public class Stack {
    List<Integer> newStack = new List<Integer>();
    
    public void push(Object obj) {
        newStack.add(0, 0);
    }
    
    public Object pop() {
        newStack.remove(0);
        return newStack;
    }
    
    public Integer size() {
        Integer s = newStack.size();
    }
    
    public Object peek(){
        newStack.get(-1);
    }
    
    public Boolean isEmpty() {
        newStack.isEmpty();
    }
}