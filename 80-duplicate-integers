public Boolean containsDuplicates(List<Integer> nums) {
    Set<Integer> result = new Set<Integer>();

    result.addAll(nums);

    if((!result.contains(nums)) return false;
    for(Integer n : nums) {
        if(!result.contains(n)){
            return false;
        }
        else{
            return true;
        }

    }
    return false;
}