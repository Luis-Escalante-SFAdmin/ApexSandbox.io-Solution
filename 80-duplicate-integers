public Boolean containsDuplicates(List<Integer> nums) {
    Set<Integer> result = new Set<Integer>();

    result.containsAll(nums); 
    //result.addAll(nums);

    for(Integer n = 0; n < nums.size(); n++) {
        if(result.contains(n)){
            return true;
        }
        else {
            return false;
        }
    }
    return false;
}