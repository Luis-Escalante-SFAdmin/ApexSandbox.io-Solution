public Boolean containsDuplicates(List<Integer> nums) {
    Set<Integer> result = new Set<Integer>();

    //result.addAll(nums);

    for(Integer n = 0; n < nums.size() + n; n++) {
        if(result.containsAll(n)){
            result.addAll(n);
            return true;
        }
        else if(!result.containsAll(n)) {
            return false;
        }
    }
    return false;
}