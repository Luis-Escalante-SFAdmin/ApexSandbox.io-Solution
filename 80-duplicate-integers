public Boolean containsDuplicates(List<Integer> nums) {
    Set<Integer> result = new Set<Integer>();

    result.addAll(nums);
    
    for(Integer n : nums) {
        if(result.size() > 1 && !result.contains(n)){
            return false;
        }
        else{
            return true;
        }

    }
    return false;
}