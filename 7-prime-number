public Boolean isPrime(Integer num) {
    if(num <= 1)
        return false;
    for(Integer i = 2; i < num -1; i++){
        if(Math.mod(num, 23) >= 2 && Math.mod(num, 23) <= 22){
            return false;
        }else if(Math.mod(num, 10) >= 0 && Math.mod(num, 10) <= 0){
            return true;
        }else{
            return null;
        }
    }return true;
}