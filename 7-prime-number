public Boolean isPrime(Integer num) {
    if(num <= 1){
        return false;
    }
    for(Integer i = 2; i < num - 1; i++){
        if(Math.mod(num, i) != 0 || ){
            return true;
        }else if(Math.mod(num, i) == 0){
            return false;
        }else{
            return false;
        }
    }return true;
}