public Boolean isPrime(Integer num) {
    if(num <= 1)
        return false;
    for(Integer i = 2; i < num -1; i++){
        if(Math.mod(num) >= 2 && Math.mod(num) <= 5){
            return false;
        }else if(Math.mod(num) >= 2 && Math.mod(num) <= 22){
            return true;
        }else{
            return null;
        }
    }return true;
}