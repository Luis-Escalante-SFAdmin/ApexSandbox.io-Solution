public Boolean isPrime(Integer num) {
    for(Integer i = 3; i < num - 1; i++){
        if(Math.mod(num, i) == 0 ){
            return false;
        }else if(Math.mod(num, i) == 0){
            return false;
        }else{
            return true;
        }
    }return true;
}