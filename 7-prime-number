public Boolean isPrime(Integer num) {
    
    if(Math.mod(num, i) == 0)
            return false;
    for(Integer i = 2; i < num - 1; i++){
        if(Math.mod(num, i) != 0 )
            return true;
    }
        

    return true;
}