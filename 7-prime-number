public Boolean isPrime(Integer num) {

    for(Integer i = 2; i < num -1; i++){
        if(Math.mod(num, 23) > 1 && Math.mod(num, 23)  < 23){
            return true;
        }else if(Math.mod(num, 10) > 2 && Math.mod(num, 10) < 5){
            return false;
        }else{
            return null;
        }
    }return true;
}