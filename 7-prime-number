public Boolean isPrime(Integer num) {
    for(Integer i = 2; i <= num -1; i++){
        if(Math.mod(num, 0)){
            isPrime == false;
            break;
        }
        }
        if(isPrime){
            return true;
        }else{
            return false;
        }
}