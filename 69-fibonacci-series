public List<Integer> fibonacciSeries(Integer n) {
    Integer a = 1;
    Integer b = 1;
    Integer c;

    List<Integer> f = new List<Integer>();
    if(n < 2) return null;
    
    for(Integer fib : f)
    {
        c = a + b;

        if(n > 2)
        {
        a = b;
        b = c;
        n.add(fib);
        }
    }
    
    return f;
}