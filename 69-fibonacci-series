public List<Integer> fibonacciSeries(Integer n) {
    Integer a = 1;
    Integer b = 1;
    Integer c;

    List<Integer> f = new List<Integer>();

    if(n > 2)
    {
    for(Integer i = 2; i <= n; i++)
    {
        c = a + b;
        a = b;
        b = c;
        f.add(c);
    }
    }
    
    return f;
}