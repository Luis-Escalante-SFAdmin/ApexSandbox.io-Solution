public List<Integer> fibonacciSeries(Integer n) {
    Integer a = 1;
    Integer b = 1;
    Integer c;

    List<Integer> f = new List<Integer>();
    if(n < 2) return null;
    
    if(n > 2)
    {
    for(Integer i = 0; i < n +; i++)
    {
        
        a = b;
        b = c;
        c = c + a;

    }
    }
    f.add(c);
    return f;
}