public Integer secondLargest(List<Integer> numbers) {
    numbers.sort();
    Integer maxValue = numbers[0];
    Integer minValue = numbers[1];

    for(Integer num : numbers){
        if(num.size() < maxValue){
            return maxValue;
        }
        else if(num.size() > minValue){
            return minValue;
        }
    }
    return maxValue;
}