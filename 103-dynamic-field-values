public List<String> getFieldsValue(Account acc, List<String> fields)
{

    List<String> results = new List<String>();

    for(String field : fields)
    {
        if(field != null)
        {
            field = acc.valueOf(fields);
            String result = acc.get(field);
        }
        
    }

    return results;

}