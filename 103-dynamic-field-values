public List<String> getFieldsValue(Account acc, List<String> fields)
{
    List<Account> accFields = new List<Account>();

    for(Account accounts : accFields)
    {
        String fieldValues = (String)accounts.get(fields);
    }

    return fieldValues;
   

}