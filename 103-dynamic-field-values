public List<String> getFieldsValue(Account acc, List<String> fields)
{

    List<String> results = new List<String>('Account');

    for(String field : fields)
    {
        if(field != null)
        {

            results = (String)acc.get(field);
        }
        
    }

    return results;

}