public List<Integer> evenNumbers(Integer n) {
    List<Integer> even = new List<Integer>();

    for(Integer i = 2; i < n; i++)
    {
        if(Math.mod(n, 2) == 2)
        {
            even.add(n);
        }
    }
    return even;
}