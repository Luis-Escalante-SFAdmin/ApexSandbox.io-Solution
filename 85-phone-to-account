public Map<String, Account> phoneToAccount(List<Account> accounts) {
    Map<String, Account> newMap = new Map<String, Account>();

    for(Account acc : accounts) {
        if(acc.Phone != null) {
            newMap.put(acc.Phone, acc.Name);
        }
    }
    return newMap;
}