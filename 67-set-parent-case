public void linkParent(Case c1, Case c2) {

    if((c1.CreatedDate < c2.CreatedDate) && (c1.ContactId != c2.ContactId))
    {
        c1.Id = c2.Id;
    }
    else if((c2.CreatedDate < c1.CreatedDate) && (c2.ContactId == c1.ContactId)) 
    {
        c2.Id = c1.Id;
    }
    
}